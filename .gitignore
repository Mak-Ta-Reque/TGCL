._*
!__main__.py

.DS_Store

__pycache__/

results/
.ipynb_checkpoints*


annotations.json/
karpathy/
logs.log
CLAUDE.md

# Ignore all .pth files
*.npy
*.pth

*.egg-info/
.vscode
.pth
output
outputs/
token.txt
.png
crops
.notebooks
*_omba.sh
token.txt

# Environment and config files (keep .env.example)
.env
.env_backup
.env_demo
config.env

# Miniconda installer and related
Miniconda3*
miniconda3*
./Miniconda3*
dtd
models/
yolo11x-seg.pt
/api/temp_images

# Large datasets built/downloaded locally -- regenerable via preprocessing/
# scripts, not meant to be versioned (see CLAUDE.md: everything under this
# repo already lives on the NVME mount, but git itself shouldn't carry
# multi-GB dataset trees)
data/

# Scratch space used for ad-hoc artifact-publishing builds
.scratch/

# Claude Code tool state (settings, worktrees, skills) -- local workflow
# config, not project source. Re-add a specific file with a leading "!" if
# something under here should be shared/tracked after all.
.claude/

# Accidentally-committed git-lfs client binary/docs (a tool, not project content)
git-lfs-3.5.1/